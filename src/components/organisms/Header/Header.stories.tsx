import React from 'react';
import HeaderItem from './Header';


export default {
    title: 'organism/Header',
    component: HeaderItem,
};

const Default = () => <HeaderItem />

export const Primary = Default.bind({});